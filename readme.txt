Prerequisites:
 theano(bleeding edge version) (http://deeplearning.net/tutorial/)
 opencv for python
 lasagne: a light wrap on theano (http://lasagne.readthedocs.io/en/latest/)


Alexnet:
test my pretrained models:
 run test_alexnet_ML2.py
 note:change test_path to the test image directory
      Manualy set opencv path before import
      change the path to save params

train your own models:
 run test_alexnet_ML.py
 note:change train_path to the test image directory
      Manualy set opencv path before import
      change the path to save params



Googlenet:
test my pretrained models:
 run Googlenet_ML2.py
 note:change test_path to the test image directory
      Manualy set opencv path before import
      change the path to save params

train your own models:
 run Googlenet_ML.py
 note:change train_path to the test image directory
      Manualy set opencv path before import
      change the path to save params

